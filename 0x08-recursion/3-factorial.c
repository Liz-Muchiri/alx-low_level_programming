#include "main.h"
/**
  * int factorial - returns the factorial of a given number
  * @n: number to return the form of factorial
  * Return: n factorial
  */
int factorial(int n);
{
	int (n < 0)
	return (-1);
	if (n == 0)
		return (1);
	return (n * factorial(n - 1));
}
